import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class hati here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class UI_Hati extends Actor {
    public UI_Hati(){
        GreenfootImage image = new GreenfootImage("hati.png");
        image.scale(32, 32); 
        setImage(image);
    }
    public void act(){
        
    }
}
